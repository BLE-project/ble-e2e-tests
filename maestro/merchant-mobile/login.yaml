appId: com.terrio.merchant
---
# Merchant Mobile — Login flow + BUG-003 verification

- launchApp:
    stopApp: true

- extendedWaitUntil:
    visible: "TERRIO Merchant"
    timeout: 30000

- assertVisible: "Merchant portal"

- tapOn: { id: "username-input" }
- inputText: "dev-merchant"
- hideKeyboard

- tapOn: { id: "password-input" }
- inputText: "dev-pass"
- hideKeyboard

- tapOn: { id: "login-btn" }

- tapOn:
    text: "Non ora|Never|Not now"
    optional: true

- extendedWaitUntil:
    visible: "Home|Dashboard"
    timeout: 15000
