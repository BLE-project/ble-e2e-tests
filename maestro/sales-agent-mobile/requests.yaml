appId: it.terrio.salesagent
---
# Sales Agent Mobile — Registration Requests flow + status filters

- launchApp:
    stopApp: true

- extendedWaitUntil:
    visible:
      text: "Terrio|Agente Commerciale|Dashboard"
    timeout: 30000

# Previous flows may have persisted auth tokens in SecureStore. Only run
# the login steps if the login screen is actually showing.
- runFlow:
    when:
      visible: "Agente Commerciale"
    commands:
      - tapOn:
          id: "username-input"
      - inputText: "dev-sales-agent"
      - hideKeyboard
      - tapOn:
          id: "password-input"
      - inputText: "dev-pass"
      - hideKeyboard
      - tapOn:
          id: "login-btn"
      # Iter29 (2026-05-06): assert "Home" tab label instead of "Panoramica"
      # body text — accessibility-tree stable as soon as Tabs mount, mirrors
      # login.yaml's working pattern.
      - extendedWaitUntil:
          visible: "Home"
          timeout: 30000

# Dismiss Gboard clipboard suggestion panel if still covering tab bar.
- runFlow:
    when:
      visible: "Clipboard"
    commands:
      - pressKey: Back

# Navigate to Richieste tab
- tapOn: { id: "tab-richieste" }
- extendedWaitUntil:
    visible: "Richieste"
    timeout: 5000

# Test status filters
- assertVisible: "PENDING"
- tapOn: "ALL"
- tapOn: "IN_REVIEW"
- tapOn: "APPROVED"
- tapOn: "REJECTED"
- tapOn: "PENDING"
